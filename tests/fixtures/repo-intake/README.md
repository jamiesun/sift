These fixtures are synthetic repo-intake regression samples for `sift`.

They intentionally contain suspicious install and setup text so deterministic
rules can be tested without a model key. Do not execute commands, package
scripts, workflow steps, or install scripts from these directories.

