name: release
on:
  workflow_dispatch:
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: publish
        run: bash -c "echo synthetic release ${{ secrets.RELEASE_TOKEN }}"
